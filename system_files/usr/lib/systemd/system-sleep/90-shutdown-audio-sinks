#!/bin/bash
# Forces PipeWire sinks to suspend before s2idle to allow Qualcomm ADSP to collapse.

case "$1" in
    pre)
        systemctl --user --machine="armada@" stop wireplumber.service
        ;;
    post)
        systemctl --user --machine="armada@" start wireplumber.service
        ;;
esac
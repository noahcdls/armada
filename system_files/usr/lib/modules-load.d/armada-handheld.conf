hid_steam
hid_nintendo
hid_playstation
hid_sony
# armada-input-calibration needs controller parameters present before InputPlumber.
rsinput
retroid
mangmi_pocket_max

# arm64 defconfig builds conservative as a module (CPU_FREQ_GOV_CONSERVATIVE=m),
# so nothing registers it and scaling_available_governors never lists it.
cpufreq_conservative
